# Jars des plugins tiers dont ce collecteur dépend à la COMPILATION.
# Une ligne par URL. Aucun jar tiers n'est embarqué dans le jar produit.
# (aucun — ce module ne dépend que de paper-api/velocity-api)
